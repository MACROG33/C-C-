#include<stdio.h>
int main()
{
    int n,num;
    printf("N:");
    scanf("%d",&n);
    for(int i=1;i<=n;i++)
    {
        printf("NUMBER:");
        scanf("%d",&num);
    }

    return 0;
}
